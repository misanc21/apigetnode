# Assesment to get a Api call. 
you can find the solution in index js, clone the repo, and run this on the command line

```bash
npm i
```

Once you get all the modules installed run it

```bash
node index.js
```

Also, you can take the function and post it in the assesment.

THX.
